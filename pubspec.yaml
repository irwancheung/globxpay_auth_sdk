name: globxpay_auth_sdk
description: "GolobXpay Registration SDK for Flutter"
version: 0.0.2
homepage: https://devoyard.visualstudio.com/_git/GlobxPay.SDK

environment:
  sdk: ^3.10.3
  flutter: ">=3.3.0"

dependencies:
  sizer: ^3.1.3
  flutter_svg: any
  dropdown_button2: 3.0.0-beta.6
  cookie_jar: ^4.0.9
  dio_cookie_manager: ^3.4.0
  searchable_paginated_dropdown: ^1.3.0
  dio: ^5.9.2
  talker_dio_logger: ^5.1.17
  animated_snack_bar: any
  intl: any
  idwise_flutter_sdk: ^6.7.6
  intl_phone_field: ^3.2.0
  lottie: any
  mixpanel_flutter: ^2.8.0
  provider: ^6.1.5+1
  image_picker: ^1.2.2
  #flutter_alice: ^2.0.1
  device_info_plus: ^13.1.0

  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.1.8
  flutter_inappwebview: ^6.1.5
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.globxpay.auth.globxpay_auth_sdk
        pluginClass: GlobxpayAuthSdkPlugin
      ios:
        pluginClass: GlobxpayAuthSdkPlugin

  # To add assets to your plugin package, add an assets section, like this:
  assets:
    - assets/images/
    - assets/images/id_documents/
    - assets/svg/
    - assets/lottie/
    - assets/terms_and_conditions/
    - assets/recaptcha.html
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
